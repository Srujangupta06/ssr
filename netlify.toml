[build]
  command = "next build"
  publish = "out"   # Next.js static export folder

# Redirect all routes to index.html for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
